{
  "name": "real-estate-listing-platform",
  "version": "1.0.0",
  "description": "Full-stack Real Estate Listing & Property Management Platform",
  "scripts": {
    "start": "node server/index.js",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev",
    "dev": "concurrently -n \"SERVER,CLIENT\" -c \"cyan,magenta\" \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix client run build",
    "deploy": "npm --prefix client run build && npx gh-pages -d client/dist",
    "test": "node server/test.js"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
